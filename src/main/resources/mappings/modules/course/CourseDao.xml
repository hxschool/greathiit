<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.course.dao.CourseDao">
    
	<sql id="courseColumns">
		a.id AS "id",
		a.curs_num AS "cursNum",
		a.curs_name AS "cursName",
		a.curs_eng_name AS "cursEngName",
		a.curs_major AS "cursMajor",
		a.curs_week_total AS "cursWeekTotal",
		a.curs_week_total AS "cursWeekHour",
		a.curs_class_hour AS "cursClassHour",
		a.curs_credit AS "cursCredit",
		a.curs_curr_term AS "cursCurrTerm",
		a.curs_intro AS "cursIntro",
		a.curs_note1 AS "cursNote1",
		a.curs_note2 AS "cursNote2",
		a.curs_pre_courses AS "cursPreCourses",
		a.curs_property AS "cursProperty",
		a.curs_select_course_type as "cursSelectCourseType",
		a.curs_term AS "cursTerm",
		a.curs_type AS "cursType",
		a.curs_total AS "cursTotal",
		a.curs_form AS "cursForm",
		a.curs_status AS "cursStatus",
		a.curs_learning_model AS "cursLearningModel",
		a.teacher_number AS "teacher.no",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag",
		b.name as "teacher.name"
	</sql>
	
	<sql id="courseJoins">
	LEFT JOIN sys_user b on a.teacher_number=b.no
	</sql>
    
	<select id="get" resultType="Course">
		SELECT 
			<include refid="courseColumns"/>
		FROM course a
		<include refid="courseJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="getCourse" resultType="Course">
		SELECT
		<include refid="courseColumns" />
		FROM course a 
		<include refid="courseJoins" />
		<where>
			1=1 and
			a.del_flag = #{DEL_FLAG_NORMAL}
	
			<if test="teacher.no!=null and teacher.no!=''">
				and a.teacher_number=#{teacher.no}
			</if>
	
			<if test="cursName!=null and cursName!=''">
				and a.curs_name=#{cursName}
			</if>
	
			<if test="cursTerm!=null and cursTerm!=''">
				and a.curs_term =#{cursTerm}
			</if>
	
			<if test="cursNum!=null and cursNum!=''">
				and a.curs_num=#{cursNum}
			</if>
		</where>
	</select>
	
	<select id="findList" resultType="Course">
		SELECT 
			<include refid="courseColumns"/>
		FROM course a
		<include refid="courseJoins"/>
		<where>
		1=1 and
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="item!=null and item!='' and item.size() > 0">
			and a.curs_major in 
			<foreach collection="item" index="index" item="item" open="(" separator="," close=")">
            	#{item}
        	</foreach>
			</if>
			<if test="teacher!=null and teacher.no!=null and teacher.no!=''">
			and b.no=#{teacher.no}
			</if>
			<if test="teacher!=null and teacher.name!=null and teacher.name!=''">
			and b.name=#{teacher.name}
			</if>
			<if test="cursStatus!=null and cursStatus!=''">
			and a.curs_status=#{cursStatus}
			</if>
			<if test="cursName!=null and cursName!=''">
			and a.curs_name  LIKE concat(concat('%',#{cursName}),'%')
			</if>
			
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findListByStudentNumber" resultType="Course">
		SELECT 
			<include refid="courseColumns"/>
		FROM course a
		<include refid="courseJoins"/>
		<where>
		1=1 and
			a.del_flag = #{DEL_FLAG_NORMAL}
			and sc_lock='2' and right(course_class,8)=#{studentNumber}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	
	<select id="findCoursesByPaike" resultType="Course">
		SELECT
		<include refid="courseColumns" />
		FROM course a
		<include refid="courseJoins" />
		<where>
			1=1 and
			a.del_flag = #{DEL_FLAG_NORMAL}

		</where>
		ORDER BY a.update_date DESC
	</select>
	
	
	<select id="findAllList" resultType="Course">
		SELECT 
			<include refid="courseColumns"/>
		FROM course a
		<include refid="courseJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO course(
			id,
			curs_num,
			curs_name,
			curs_eng_name,
			curs_major,
			curs_week_total,
			curs_week_hour,
			curs_class_hour,
			curs_credit,
			curs_curr_term,
			curs_intro,
			curs_note1,
			curs_note2,
			curs_pre_courses,
			curs_property,
			curs_select_course_type,
			curs_term,
			curs_type,
			curs_total,
			curs_form,
			curs_status,
			curs_learning_model,
			teacher_number,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{cursNum},
			#{cursName},
			#{cursEngName},
			#{cursMajor},
			#{cursWeekTotal},
			#{cursWeekHour},
			#{cursClassHour},
			#{cursCredit},
			#{cursCurrTerm},
			#{cursIntro},
			#{cursNote1},
			#{cursNote2},
			#{cursPreCourses},
			#{cursProperty},
			#{cursSelectCourseType},
			#{cursTerm},
			#{cursType},
			#{cursTotal},
			#{cursForm},
			#{cursStatus},
			#{cursLearningModel},
			#{teacher.no},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE course SET 	
			curs_num = #{cursNum},
			curs_name = #{cursName},
			curs_eng_name = #{cursEngName},
			curs_major = #{cursMajor},
			curs_week_total = #{cursWeekTotal},
			curs_week_hour = #{cursWeekHour},
			curs_class_hour = #{cursClassHour},
			curs_credit = #{cursCredit},
			curs_curr_term = #{cursCurrTerm},
			curs_intro = #{cursIntro},
			curs_note1 = #{cursNote1},
			curs_note2 = #{cursNote2},
			curs_pre_courses = #{cursPreCourses},
			curs_property = #{cursProperty},
			curs_select_course_type = #{cursSelectCourseType},
			curs_term = #{cursTerm},
			curs_type = #{cursType},
			curs_total = #{cursTotal},
			curs_status = #{cursStatus},
			curs_learning_model = #{cursLearningModel},
			curs_form = #{cursForm},
			teacher_number = #{teacher.no},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE course SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>