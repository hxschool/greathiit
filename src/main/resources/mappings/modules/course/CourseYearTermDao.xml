<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.course.dao.CourseYearTermDao">
    
	<sql id="courseYearTermColumns">
		a.id AS "id",
		a.year_term AS "yearTerm",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="courseYearTermJoins">
	</sql>
	<select id="systemConfig" resultType="CourseYearTerm">
		SELECT <include refid="courseYearTermColumns"/> FROM course_year_term a order by a.update_date desc limit 1
    </select>
	<select id="get" resultType="CourseYearTerm">
		SELECT 
			<include refid="courseYearTermColumns"/>
		FROM course_year_term a
		<include refid="courseYearTermJoins"/>
		WHERE  a.year_term = #{yearTerm}
		
	</select>

	<select id="findList" resultType="CourseYearTerm">
		SELECT 
			<include refid="courseYearTermColumns"/>
		FROM course_year_term a
		<include refid="courseYearTermJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="CourseYearTerm">
		SELECT 
			<include refid="courseYearTermColumns"/>
		FROM course_year_term a
		<include refid="courseYearTermJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO course_year_term(
			id,
			year_term,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{yearTerm},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE course_year_term SET 	
			year_term = #{yearTerm},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE course_year_term SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE  a.year_term = #{yearTerm}
	</update>
	
</mapper>