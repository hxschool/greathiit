<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.out.jcd.dao.RsMajorSetupDao">
    
	<sql id="rsMajorSetupColumns">
		a.id AS "id",
		a.major_id AS "majorId",
		a.major_name AS "majorName",
		a.major_count AS "majorCount",
		a.major_total AS "majorTotal",
		a.status AS "status",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="rsMajorSetupJoins">
	</sql>
    
	<select id="get" resultType="RsMajorSetup">
		SELECT 
			<include refid="rsMajorSetupColumns"/>
		FROM rs_major_setup a
		<include refid="rsMajorSetupJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="getMajorId" resultType="RsMajorSetup">
		SELECT 
			<include refid="rsMajorSetupColumns"/>
		FROM rs_major_setup a
		<include refid="rsMajorSetupJoins"/>
		WHERE a.major_id = #{majorId}
	</select>
	

	
	<select id="findList" resultType="RsMajorSetup">
		SELECT 
			<include refid="rsMajorSetupColumns"/>
		FROM rs_major_setup a
		<include refid="rsMajorSetupJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="majorId != null and majorId != ''">
				AND a.major_id = #{majorId}
			</if>
			<if test="majorName != null and majorName != ''">
				AND a.major_name = #{majorName}
			</if>
			<if test="majorCount != null and majorCount != ''">
				AND a.major_count = #{majorCount}
			</if>
			<if test="majorTotal != null and majorTotal != ''">
				AND a.major_total = #{majorTotal}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="RsMajorSetup">
		SELECT 
			<include refid="rsMajorSetupColumns"/>
		FROM rs_major_setup a
		<include refid="rsMajorSetupJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO rs_major_setup(
			major_id,
			major_name,
			major_count,
			major_total,
			status,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{majorId},
			#{majorName},
			#{majorCount},
			#{majorTotal},
			#{status},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE rs_major_setup SET 	
			major_id = #{majorId},
			major_name = #{majorName},
			major_count = #{majorCount},
			major_total = #{majorTotal},
			status = #{status},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE rs_major_setup SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>